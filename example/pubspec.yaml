name: example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.0-10.0.dev <3.0.0"


dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.27.2
  beamer: ^1.0.0-pre.15.0
  navigator_reactive_guards: 
    path: ../


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
